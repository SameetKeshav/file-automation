[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "file-automation"
version = "0.1.0"
dependencies = [
  "pyyaml",
  "watchdog",
  "typer",
  "rich"
]

[tool.setuptools.packages.find]
where = ["src"]
